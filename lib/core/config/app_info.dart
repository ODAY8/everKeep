/// The app's name and version as shown in "About". Keep [appVersion] equal to the
/// version in pubspec.yaml — a test enforces it, so a release can't ship with a
/// stale number.
const String appName = 'Everkeep';
const String appVersion = '1.0.0';
